<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gdu.app.dao.BoardMapper">

  <select id="getBoardList" resultType="BoardDto">
    SELECT NO, TITLE, AUTHOR, POSTDATE, HIT
      FROM MVC_BOARD_T
     ORDER BY NO DESC
  </select>
  
  <select id="getBoard" parameterType="int" resultType="BoardDto">
    SELECT NO, AUTHOR, POSTDATE, IP, HIT, TITLE, CONTENT
      FROM MVC_BOARD_T
     WHERE NO = #{no}
  </select> 
  
  <insert id="addBoard" parameterType="BoardDto">
    INSERT INTO MVC_BOARD_T (
    	NO
      , AUThOR
      , TITLE
      , CONTENT
      , POSTDATE
    ) VALUES (
        MVC_BOARD_SEQ.NEXTVAL
      , #{author}
      , #{title}
      , #{content}
      , SYSDATE
    )
  </insert>
  <delete id="deleteBoard" parameterType="int">
	DELETE
	  FROM MVC_BOARD_T 
	 WHERE NO = #{no}
  </delete>
</mapper>
